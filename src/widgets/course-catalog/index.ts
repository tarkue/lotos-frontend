import { CourseCatalogWrapper } from "./ui/catalog";
import { CourseCatalogList } from "./ui/catalog-list";

export const CourseCatalog = Object.assign(
  {},
  {
    Wrapper: CourseCatalogWrapper,
    List: CourseCatalogList,
  }
);
